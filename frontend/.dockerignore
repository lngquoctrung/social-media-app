# frontend/.dockerignore

# Bỏ qua node_modules để tránh copy vào container
node_modules
npm-debug.log
.DS_Store

# Bỏ qua build nếu đã có sẵn, sẽ được build lại trong container
build

# Bỏ qua file log
*.log

# Bỏ qua file cấu hình IDE/editor
.vscode
.idea

# Bỏ qua các tệp backup và cache
*.swp
*.swo
*.bak
*.tmp

# Bỏ qua file .env nếu không muốn copy vào Docker
.env
